module {!pack!} {
    export class {!className!} extends {!classType!} {
{!vars!}
        constructor(){ super()}
        createChildren():void {
        {!viewClassMap!}
            super.createChildren();
            loadUI("{!uiPath!}");
        }
    }
}
